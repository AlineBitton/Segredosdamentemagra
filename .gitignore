node_modules/
dist/
.wrangler/
.DS_Store
*.log
.env
.env.*

# fontes subsetadas SAO versionadas (build nao depende de Python)
!public/fonts/*.woff2
# originais das provas: pesados e substituiveis pelos exports do Canva
# (mantidos fora do repo; public/img/provas/ carrega as versoes otimizadas)
src/img-originais/

# Artefatos de verificacao: 16 MB, regeneraveis em segundos.
# npm run render | npm run medir
docs/capturas/
docs/medicao/

# previa autocontida para revisao: 500 KB, regeneravel com npm run previa
segredos-da-mente-magra-previa.html
